// Tauri commands act as the IPC layer between React and Rust.
// Commands remain thin — they receive input, call services, return results.
// Populated in Phase 3+ when membership plans and member management are implemented.
