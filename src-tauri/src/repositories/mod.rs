// Repository layer contains SQL queries and database operations.
// Repositories are responsible for persistence only — no business decisions.
// Populated in Phase 3+ when membership plans and member management are implemented.
