// Service layer contains business logic.
// Each service is responsible for business rules, validation, and coordinating repositories.
// Populated in Phase 3+ when membership plans and member management are implemented.
